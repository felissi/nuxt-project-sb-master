import { expect, describe, test } from "vitest";

describe("should fill the default for para = {}", async() => {
  test("should transform the first layer", async() => {});
  test("should transform deeply", async() => {});
});
describe("should fill the default for para is partially filled", async() => {
  test("should transform the first layer", async() => {});
  test("should transform deeply", async() => {});
});
